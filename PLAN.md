# SQL 一周突击计划 

## 先给结论
- **目标可达**。你不是零基础：Access/SQL 清洗 + K-means 特征工程 + A/B 实验 + DoWhy，说明**逻辑思维与统计已到位**，缺的只是 SQL 的"固定套路"。
- SQL 面试是**有限套路**，不像算法无边界。7 天 × 3h+ 完全够冲刺。
- 关键策略：**只练高频四大题型**，不铺开看书。

## 一周排期（每天 3h+）

| 天 | 主题 | 要掌握的套路 | 当天产出 |
|---|---|---|---|
| **Day 1** | 环境 + 基础 | 建表、JOIN、GROUP BY、CASE WHEN、子查询 | 跑通 check.py，手写题3(TopN) |
| **Day 2** | 窗口函数（核心） | ROW_NUMBER / RANK / DENSE_RANK、SUM/AVG OVER、PARTITION BY | 题1的"差值"部分独立写出来 |
| **Day 3** | 日期处理 + CTE | 首单日、日期±N天、月份差、WITH 分层 | 独立写题1完整版 |
| **Day 4** | **Cohort 留存** | 首单月→月份差→CASE 透视，背死这个模板 | 独立写题2，能默写 |
| **Day 5** | 进阶难题 | 连续登录(日期-行号)、累计/移动平均、去重 | 题4 + 补充练习 |
| **Day 6** | A/B 实验 + 归因 | 实验周期确定、样本量、分流、显著性、归因模型 | 复盘项目，能口述订单归因模型 |
| **Day 7** | 模拟 + 复盘 | 限时手写全部4题（不看答案） | 盲写一遍全对 |

## 四大高频题型（押题重点）
1. **TopN / 排名** → DENSE_RANK（笔试会考）
2. **激活N日 / 留存 / Cohort** → 首单日 + 日期差 + CASE 透视（笔试最高频）
3. **同组均值对比 / 差值** → avg() over (partition by)（**面试常考**）
4. **连续活跃 / 去重** → 日期 - row_number

## 使用方法
```bash
cd D:\GithubProject\sql_sprint\sql_sprint
python app.py              # 浏览器练习：写 SQL、分层提示、自动判题
python check.py            # 终端看全部参考答案输出
python check.py hint 1     # 第1题分层提示
python check.py 1 my.sql   # 用你的答案文件判题
```

## 关于概率题（甲箱乙箱）
这是**全概率公式**（不是贝叶斯）：
- 从甲(3正3次)取3件放入乙(原3正)：
  - 取0次品(3正): C(3,3)/C(6,3)=1/20 → 乙此后 3正+3正=6正0次 → 抽到次品概率0
  - 取1次品(2正1次): C(3,2)C(3,1)/C(6,3)=9/20 → 乙=5正1次 → 抽次品 1/6
  - 取2次品(1正2次): 9/20 → 乙=4正2次 → 抽次品 2/6
  - 取3次品(0正3次): 1/20 → 乙=3正3次 → 抽次品 3/6
- 总概率 = 1/20×0 + 9/20×1/6 + 9/20×2/6 + 1/20×3/6 = **(0+9+18+3)/(120) = 30/120 = 1/4**
- 巧记：乙箱最终6件，次品期望数 = 3×(3/6)=1.5？不对，应=0×1/20+1×9/20+2×9/20+3×1/20=30/20=1.5，期望1.5/6=**1/4**。✅

## 提醒
- 面试官提到 `avg() over (partition by)` 是在**给你提示**，说明他们看重窗口函数熟练度 → Day 2 重点砸。
- 你的项目(订单归因、A/B 实验)会被深挖，Day 6 把 A/B 设计 + 归因逻辑串成 2 分钟讲述。
- 不要追求刷题量，**每题必须能盲写**才算过。
